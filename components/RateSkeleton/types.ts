export type TRateSkeletonProps = {
    uniqueKey: string;
    speed: number;
    width: number;
    height: number;
    bgColor: `#${string}` | "transparent";
    foregroundColor: `#${string}`;
    rectWidth: number;
    rectHeight: number;
    borderRadius: number;
};